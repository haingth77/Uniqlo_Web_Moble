{
  "name": "ai-qa-orchestrator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Prototype AI QA orchestrator (Claude Agent SDK): requirement -> analysis -> manual test cases -> Playwright script -> run -> result analysis",
  "scripts": {
    "orchestrate": "tsx run.ts",
    "render": "tsx render.ts",
    "viewer": "tsx build-suite-viewer.ts"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.175",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0"
  }
}
